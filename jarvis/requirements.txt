# Core (mock mode needs only these)
rich>=13.0
requests>=2.31
python-dotenv>=1.0

# Live Google OAuth2 / Gmail connectors
google-auth>=2.23
google-auth-oauthlib>=1.1
google-api-python-client>=2.100

# Tests
pytest>=7.4
